{
  "name": "Elasticsearch",
  "install": "JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 ./gradlew localDistro --console=plain",
  "terminals": [
    {
      "name": "elasticsearch",
      "command": "JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 ./gradlew run --console=plain",
      "description": "Runs a single-node Elasticsearch dev cluster. HTTP API is served on http://localhost:9200 (basic license, security disabled by default)."
    }
  ],
  "ports": [
    { "name": "elasticsearch-http", "port": 9200 },
    { "name": "elasticsearch-transport", "port": 9300 }
  ]
}
